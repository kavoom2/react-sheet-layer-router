/**
 * Sheet Layer 외부에서 Sheet Layer 를 제어하기 위한 Hook을 구현합니다.
 *
 * - react-router, nextJs의 useLocation, useRouter 등과 달리 Sheet Context Provider 외부에서도 호출해야 합니다.
 * - Dropdown 컴포넌트와 같이 ID를 통해 Sheet Layer를 제어할 수 있어야 합니다.
 */

const useSheetLayerRouter = () => {
  return null;
};

export default useSheetLayerRouter;
